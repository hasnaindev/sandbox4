name: Validate PR

on:
  pull_request:
    types:
      - opened
      - synchronize
      - closed
    branches:
      - master
      - develop
      - fix/**
      - feature/**

jobs:
  validate:
    name: Validate PR
    uses: ./.github/workflows/calls/validate.yml
